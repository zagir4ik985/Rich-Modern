<Window x:Class="ZagaLoader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="zagaDLC" Height="420" Width="620"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" ShowInTaskbar="True" WindowStartupLocation="CenterScreen">
    <Border Background="#1a1a2e" CornerRadius="12" BorderBrush="#16213e" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" CornerRadius="12,12,0,0" Background="#0f3460" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="zagaDLC" Foreground="#e94560" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="15,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button Content="─" Width="30" Height="25" Background="Transparent" Foreground="#aaa" BorderThickness="0" Click="Minimize_Click" Cursor="Hand"/>
                        <Button Content="✕" Width="30" Height="25" Background="Transparent" Foreground="#e94560" BorderThickness="0" Click="Close_Click" Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main content -->
            <Grid Grid.Row="1" Margin="25,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="zagaDLC 1.21.11" Foreground="#e94560" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <ListBox Grid.Row="1" x:Name="LogBox" Background="#16213e" Foreground="#a0a0b0" BorderThickness="0"
                         FontFamily="Consolas" FontSize="11" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         VerticalContentAlignment="Top"/>

                <Grid Grid.Row="2" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ProgressBar Grid.Column="0" x:Name="ProgressBar" Height="8" Background="#16213e" Foreground="#e94560" BorderThickness="0" Maximum="100" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock Grid.Column="1" x:Name="StatusText" Text="Готово" Foreground="#a0a0b0" FontSize="11" VerticalAlignment="Center"/>
                </Grid>

                <TextBlock Grid.Row="3" x:Name="MCPathText" Text="" Foreground="#555" FontSize="9" TextWrapping="Wrap" Margin="0,8,0,0"/>

                <Button Grid.Row="4" x:Name="LaunchButton" Content="ЗАПУСТИТЬ" Height="42" Margin="0,12,0,0"
                        Background="#e94560" Foreground="White" FontSize="16" FontWeight="Bold"
                        BorderThickness="0" Cursor="Hand" Click="LaunchButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#c73650"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="#555"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
